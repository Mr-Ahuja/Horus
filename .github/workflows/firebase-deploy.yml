name: Firebase Hosting

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  deploy:
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate env.js for client
        run: |
          cat > env.js << 'EOF'
          window.ENV = {
            apiKey: "${{ vars.FIREBASE_API_KEY }}",
            authDomain: "${{ vars.FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ vars.FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ vars.FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ vars.FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ vars.FIREBASE_APP_ID }}",
            measurementId: "${{ vars.FIREBASE_MEASUREMENT_ID }}"
          };
          EOF

      - name: Deploy preview on PRs
        if: github.event_name == 'pull_request'
        uses: w9jds/firebase-action@v3.0.0
        with:
          args: hosting:channel:deploy pr-${{ github.event.number }} --project horus-vision-4587f --expires 7d
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

      - name: Deploy to production on main
        if: github.event_name == 'push'
        uses: w9jds/firebase-action@v3.0.0
        with:
          args: deploy --only hosting --project horus-vision-4587f
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
